import * as React from 'react';
import { View, Text } from 'react-native';

export interface CategoriarScreenProps {
}

export function CategoriarScreen (props: CategoriarScreenProps) {
    return (
      <View>
         <Text>CategoriarScreen</Text>
      </View>
    );
}
