import * as React from 'react';
import { View, Text } from 'react-native';

export interface InfantilScreenProps {
}

export function InfantilScreen (props: InfantilScreenProps) {
    return (
      <View>
         <Text>InfantilScreen</Text>
      </View>
    );
}
